# YogaTracker

Next.js app — see the [root README](../README.md) for setup.

```bash
npm run dev          # http://localhost:3000
npm run setup:supabase
```

Copy `.env.local.example` to `.env.local` and add Supabase keys.
